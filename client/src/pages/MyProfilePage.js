import { Link } from 'react-router-dom';

// TODO: If user update profile check userID and currentID
// IF user delete profile check userCurrentID and role, maybe it's admin

export const MyProfilePage = () => {
    return ( <div>
        <h1>
            My Profile
        </h1>
    </div> );
};